name: auto-pr
run-name: '🤖 Auto PR - ${{ github.ref_name }}'

on:
  push:
    branches:
      - feat/**
      - fix/**
      - hotfix/**
      - release/**
      - refactor/**
      - chore/**
      - docs/**
      - style/**
      - test/**
      - ci/**
      - perf/**

permissions:
  contents: read

jobs:
  auto-pr:
    name: Auto PR
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 💻
        uses: actions/checkout@v4.1.7

      - name: Create PR 🚀
        uses: actions/github-script@v7.0.1
        with:
          script: |
            
            console.log("REF_NAME_CTX:", `${{ github.ref_name}}`)
            console.log("REPO_CTX:", `${{ github.repository }}`)
            console.log(context.payload)
            
            const msg = context.payload.head_commit.message;                // get latest commit on the branch
            let [title, body] = msg.split('\n').filter((x) => !!x);         // split the commit message into title and body
            
            console.log("PAYLOAD:", {
              "owner": context.repo.owner
            });

#            let [owner, repoName...] = `${{ github.repository }}`.split('/');  // get the owner and repo name
