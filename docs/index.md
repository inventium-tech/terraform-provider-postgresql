---
page_title: "Postgresql"
subcategory: ""
description: |-
    The PostgreSQL Provider is used to manage PostgreSQL resources such as roles, databases, and event triggers.
---

# PostgreSQL Provider

The PostgreSQL Provider is used to manage PostgreSQL resources such as roles, databases, and event triggers.

There are a few things that make this provider stand out:

* It's built using the latest approach suggested by HashiCorp, using
  the [Terraform Plugin Framework](https://developer.hashicorp.com/terraform/plugin/framework).
* Instead of replacing existing resources from other providers, we decided to start by covering the missing parts (e.g.
  event triggers) and then expand to other resources.
* Focused on best practices and security, the next short-term goal is to provide ephemeral resources and write-only
  arguments while managing PostgreSQL roles/users.

## Roadmap

Here you can find a list of resources, data-sources and functions in our Roadmap with their current status:

| Name          | Resource | Data Source |
|---------------|:--------:|:-----------:|
| Event Trigger |    ✅     |      ✅      |
| Functions     |    ✅     |     🔜      |
| Role          |    ✅     |     🔜      |
| Database      |    🔜    |     🔜      |
| Schema        |    🔜    |     🔜      |

## Example Usage

```terraform
terraform {
  required_providers {
    postgresql = {
      source = "registry.terraform.io/inventium-tech/postgresql"
    }
  }
}

provider "postgresql" {
  host     = "localhost"
  port     = 5432
  username = "postgres"
  password = "password"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `database` (String) The name of the PostgreSQL database to connect to. May be set via the environment variable `POSTGRES_DATABASE`.
- `host` (String) The hostname of the PostgreSQL server. May be set via the environment variable `POSTGRES_HOST`.
- `password` (String, Sensitive) The password to use when connecting to the PostgreSQL server. May be set via the environment variable `POSTGRES_PASSWORD`.
- `port` (Number) The port of the PostgreSQL server. May be set via the environment variable `POSTGRES_PORT`. (default: 5432)
- `scheme` (String) The scheme to use when connecting to the PostgreSQL database. The value must be one of the following:
	* 'postgres' (default)
	* 'gcppostgres'	
	* 'awspostgres'
May be set via the environment variable 'POSTGRES_SCHEME'. (default: 'postgres')
- `sslmode` (String) The SSL mode to use when connecting to the PostgreSQL server. The value must be one of the following:
	* 'disable' (No SSL)
	* 'require' (*default*. Always SSL, skip verification)
	* 'verify-ca' (Always SSL, verify that the server certificate is issued by a trusted CA)
	* 'verify-full' (Always SSL, *same as 'verify-ca', plus the server host name matches the one in the certificate).
May be set via the environment variable 'POSTGRES_SSLMODE'. (default: 'disable')
- `username` (String) The username to use when connecting to the PostgreSQL server. May be set via the environment variable `POSTGRES_USERNAME`.


